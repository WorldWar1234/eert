import sharp from"sharp";import{redirect}from"./rrd.js";export async function compressImg(request,reply,imgData){const{webp,grayscale,quality,originSize}=request.params;const imgFormat=webp?"webp":"jpeg";try{let sharpInstance=sharp(imgData).grayscale(grayscale).toFormat(imgFormat,{quality:quality,progressive:true,optimizeScans:webp,chromaSubsampling:webp?"4:4:4":"4:2:0"});const{data,info}=await sharpInstance.toBuffer({resolveWithObject:true});reply.header("content-type",`image/${imgFormat}`).header("content-length",info.size).header("x-original-size",originSize).header("x-bytes-saved",originSize-info.size).code(200).send(data)}catch(error){return redirect(request,reply)}}
